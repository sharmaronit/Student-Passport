{
  "scripts": {
    "build": "cd frontend && npm install && npm run build && rm -rf ../.vercel && mv .vercel ../.vercel"
  },
  "dependencies": {
    "framer-motion": "^12.42.2",
    "lucide-react": "^1.23.0"
  }
}
